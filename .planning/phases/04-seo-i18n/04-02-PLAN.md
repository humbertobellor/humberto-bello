---
phase: 04-seo-i18n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en.json
  - src/i18n/locales/es.json
  - src/i18n/locales/de.json
  - public/robots.txt
  - public/sitemap.xml
  - scripts/build.mjs
autonomous: true
requirements: [SEO-01, SEO-02, SEO-05, SEO-06]

must_haves:
  truths:
    - "Each locale JSON has seo.* keys with locale-specific meta content"
    - "robots.txt points to humbertobellor.github.io/dossier/sitemap.xml"
    - "Build generates dist/sitemap.xml with hreflang annotations for all 3 locales"
    - "Sitemap has x-default pointing to English"
    - "Sitemap uses correct XML namespace for hreflang"
    - "JSON-LD in all locale files has updated URLs (humbertobellor.github.io/dossier/)"
  artifacts:
    - path: "src/i18n/locales/en.json"
      provides: "English locale with seo.* keys"
      contains: "seo"
    - path: "src/i18n/locales/es.json"
      provides: "Spanish locale with seo.* keys"
      contains: "seo"
    - path: "src/i18n/locales/de.json"
      provides: "German locale with seo.* keys"
      contains: "seo"
    - path: "dist/sitemap.xml"
      provides: "Generated sitemap with hreflang"
      contains: "hreflang"
    - path: "public/robots.txt"
      provides: "Updated robots.txt with new sitemap URL"
      contains: "humbertobellor.github.io"
  key_links:
    - from: "scripts/build.mjs"
      to: "dist/sitemap.xml"
      via: "sitemap generation function"
      pattern: "writeFileSync.*sitemap\\.xml"
    - from: "dist/sitemap.xml"
      to: "src/i18n/locales/*.json"
      via: "locale list drives sitemap entries"
      pattern: "locales\\.forEach"
---

<objective>
SEO metadata per locale and sitemap generation. Add seo.* keys to all 3 locale JSON files, update robots.txt, extend build script to generate sitemap.xml with hreflang annotations.

Purpose: Search engines need per-locale meta tags, structured data, and a sitemap with hreflang to correctly index the multilingual site.
Output: Updated locale JSONs with seo.* keys, updated robots.txt, build-generated sitemap.xml.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-i18n/04-CONTEXT.md
@.planning/phases/04-seo-i18n/04-UI-SPEC.md

<interfaces>
<!-- Key patterns the executor needs. -->

From src/i18n/locales/en.json (current structure — top-level keys only):
```json
{
  "nav": {...},
  "hero": {...},
  "skills": {...},
  "experience": {...},
  "clients": {...},
  "cta": {...},
  "footer": {...}
}
```

From 04-UI-SPEC.md (seo.* key contract):
```json
{
  "seo": {
    "title": "Humberto Bello · AI Architect & Engineering Leader | Agentic AI · RAG · Cloud",
    "description": "AI Architect & Engineering Leader, 20+ years in agentic AI, RAG systems, cloud-native platforms. Open to AI architect, engineering director, solopreneur roles.",
    "ogTitle": "Humberto Bello · AI Architect & Engineering Leader",
    "ogDescription": "AI Architect & Engineering Leader, 20+ years in agentic AI, RAG systems, cloud-native platforms.",
    "twitterTitle": "Humberto Bello · AI Architect & Engineering Leader",
    "twitterDescription": "AI Architect & Engineering Leader, 20+ years in agentic AI, RAG systems, and cloud-native platforms.",
    "ogLocale": "en_US"
  }
}
```

From 04-UI-SPEC.md (locale-specific values):
| Key | EN | ES | DE |
|-----|----|----|-----|
| seo.title | Humberto Bello · AI Architect & Engineering Leader \| Agentic AI · RAG · Cloud | Humberto Bello · Arquitecto de IA y Líder de Ingeniería \| IA Agéntica · RAG · Cloud | Humberto Bello · KI-Architekt & Engineering Leader \| Agentische KI · RAG · Cloud |
| seo.description | AI Architect & Engineering Leader, 20+ years in agentic AI... | Arquitecto de IA y Líder de Ingeniería, más de 20 años... | KI-Architekt & Engineering Leader, 20+ Jahre Erfahrung... |
| seo.ogLocale | en_US | es_ES | de_DE |

From scripts/build.mjs (current build — needs sitemap generation added):
```javascript
// Current script ends at line 121 with console.log
// Sitemap generation should be added after locale HTML generation
```

From public/robots.txt (current):
```
User-agent: *
Allow: /

Sitemap: https://humbertobello.replit.app/sitemap.xml
```

From public/sitemap.xml (current — 1 URL, replit.app):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://humbertobello.replit.app/</loc>
    <lastmod>2026-05-18</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Add seo.* keys to all 3 locale JSON files and update robots.txt</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/es.json, src/i18n/locales/de.json, public/robots.txt</files>
  <read_first>
    src/i18n/locales/en.json
    src/i18n/locales/es.json
    src/i18n/locales/de.json
    public/robots.txt
  </read_first>
  <action>
**A. src/i18n/locales/en.json — Add seo block (per D-03, D-15):**

Add a top-level `"seo"` key after the existing `"footer"` key. The full seo block for English:
```json
"seo": {
  "title": "Humberto Bello · AI Architect & Engineering Leader | Agentic AI · RAG · Cloud",
  "description": "AI Architect & Engineering Leader, 20+ years in agentic AI, RAG systems, cloud-native platforms. Open to AI architect, engineering director, solopreneur roles.",
  "ogTitle": "Humberto Bello · AI Architect & Engineering Leader",
  "ogDescription": "AI Architect & Engineering Leader, 20+ years in agentic AI, RAG systems, cloud-native platforms.",
  "twitterTitle": "Humberto Bello · AI Architect & Engineering Leader",
  "twitterDescription": "AI Architect & Engineering Leader, 20+ years in agentic AI, RAG systems, and cloud-native platforms.",
  "ogLocale": "en_US"
}
```

**B. src/i18n/locales/es.json — Add seo block (per D-03, D-15):**

```json
"seo": {
  "title": "Humberto Bello · Arquitecto de IA y Líder de Ingeniería | IA Agéntica · RAG · Cloud",
  "description": "Arquitecto de IA y Líder de Ingeniería, más de 20 años en IA agéntica, sistemas RAG, plataformas cloud-native. Abierto a roles de arquitecto de IA, director de ingeniería, solopreneur.",
  "ogTitle": "Humberto Bello · Arquitecto de IA y Líder de Ingeniería",
  "ogDescription": "Arquitecto de IA y Líder de Ingeniería, más de 20 años en IA agéntica, sistemas RAG, plataformas cloud-native.",
  "twitterTitle": "Humberto Bello · Arquitecto de IA y Líder de Ingeniería",
  "twitterDescription": "Arquitecto de IA y Líder de Ingeniería, más de 20 años en IA agéntica, sistemas RAG, y plataformas cloud-native.",
  "ogLocale": "es_ES"
}
```

**C. src/i18n/locales/de.json — Add seo block (per D-03, D-15):**

```json
"seo": {
  "title": "Humberto Bello · KI-Architekt & Engineering Leader | Agentische KI · RAG · Cloud",
  "description": "KI-Architekt & Engineering Leader, 20+ Jahre Erfahrung in agentischer KI, RAG-Systemen, Cloud-native Plattformen. Offen für KI-Architekt, Engineering Director, Solopreneur-Rollen.",
  "ogTitle": "Humberto Bello · KI-Architekt & Engineering Leader",
  "ogDescription": "KI-Architekt & Engineering Leader, 20+ Jahre Erfahrung in agentischer KI, RAG-Systemen, Cloud-native Plattformen.",
  "twitterTitle": "Humberto Bello · KI-Architekt & Engineering Leader",
  "twitterDescription": "KI-Architekt & Engineering Leader, 20+ Jahre Erfahrung in agentischer KI, RAG-Systemen, und Cloud-native Plattformen.",
  "ogLocale": "de_DE"
}
```

**D. public/robots.txt — Update sitemap URL (per D-13):**

Replace the entire content with:
```
User-agent: *
Allow: /

Sitemap: https://humbertobellor.github.io/dossier/sitemap.xml
```
  </action>
  <verify>
    <automated>
# Verify seo block exists in all 3 locale files
grep -c '"seo"' src/i18n/locales/en.json src/i18n/locales/es.json src/i18n/locales/de.json

# Verify ogLocale values are correct
grep '"ogLocale"' src/i18n/locales/en.json | grep -c 'en_US'
grep '"ogLocale"' src/i18n/locales/es.json | grep -c 'es_ES'
grep '"ogLocale"' src/i18n/locales/de.json | grep -c 'de_DE'

# Verify JSON is valid
node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8')); console.log('en.json valid')"
node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/es.json','utf8')); console.log('es.json valid')"
node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/de.json','utf8')); console.log('de.json valid')"

# Verify robots.txt has new sitemap URL
grep -c 'humbertobellor.github.io/dossier/sitemap.xml' public/robots.txt
    </automated>
  </verify>
  <done>
- All 3 locale JSON files have seo.* keys with locale-specific meta content
- en.json has ogLocale: "en_US", es.json has ogLocale: "es_ES", de.json has ogLocale: "de_DE"
- All 3 JSON files parse without errors
- robots.txt points to humbertobellor.github.io/dossier/sitemap.xml
  </done>
</task>

<task type="auto">
  <name>task 2: Extend build script to generate sitemap.xml with hreflang annotations</name>
  <files>scripts/build.mjs</files>
  <read_first>
    scripts/build.mjs
    src/i18n/locales/en.json
  </read_first>
  <action>
**Extend scripts/build.mjs — Add sitemap generation (per D-10, D-11, D-12):**

Add the following sitemap generation code after the locale HTML build loop, before the completion log. This should go at the end of the try block, after the 404.html generation.

```javascript
// Generate sitemap.xml (per D-10, D-11, D-12)
const baseUrl = 'https://humbertobellor.github.io/dossier';
const now = new Date().toISOString().split('T')[0]; // YYYY-MM-DD format
const sitemapLocales = [
  { code: 'en', path: '/' },
  { code: 'es', path: '/es/' },
  { code: 'de', path: '/de/' },
];

const sitemapEntries = sitemapLocales.map(loc => {
  const hreflangLinks = sitemapLocales.map(hl =>
    `    <xhtml:link rel="alternate" hreflang="${hl.code}" href="${baseUrl}${hl.path}" />`
  ).join('\n');
  const xdefault = `    <xhtml:link rel="alternate" hreflang="x-default" href="${baseUrl}/" />`;
  return `  <url>
    <loc>${baseUrl}${loc.path}</loc>
    <lastmod>${now}</lastmod>
${hreflangLinks}
${xdefault}
  </url>`;
}).join('\n');

const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
${sitemapEntries}
</urlset>`;

writeFileSync('dist/sitemap.xml', sitemap, 'utf-8');
```

This generates a sitemap like the contract in 04-UI-SPEC.md:
- 3 `<url>` entries (en, es, de)
- Each with `<lastmod>` using build-time date
- Each with `<xhtml:link rel="alternate" hreflang="xx">` for all 3 locales
- Each with `<xhtml:link rel="alternate" hreflang="x-default">` pointing to English
- Correct XML namespaces: `xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"` + `xmlns:xhtml="http://www.w3.org/1999/xhtml"`
  </action>
  <verify>
    <automated>
# Run build to generate sitemap
node scripts/build.mjs

# Verify sitemap exists
ls -la dist/sitemap.xml

# Verify sitemap has hreflang annotations
grep -c 'hreflang' dist/sitemap.xml

# Verify sitemap has all 3 locales
grep -c '<loc>' dist/sitemap.xml

# Verify sitemap has x-default
grep -c 'hreflang="x-default"' dist/sitemap.xml

# Verify sitemap has correct namespaces
grep -c 'xmlns:xhtml' dist/sitemap.xml

# Verify sitemap URLs point to github.io
grep -c 'humbertobellor.github.io/dossier' dist/sitemap.xml

# Verify lastmod is present
grep -c '<lastmod>' dist/sitemap.xml
    </automated>
  </verify>
  <done>
- dist/sitemap.xml exists with 3 URL entries (en, es, de)
- Each entry has hreflang annotations for all 3 locales + x-default
- Sitemap uses correct XML namespaces (sitemaps.org + xhtml)
- All URLs point to humbertobellor.github.io/dossier/
- lastmod uses build-time date
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build script → sitemap.xml | Build generates sitemap from hardcoded locale list — no user input |
| robots.txt → crawlers | Static file, no dynamic content — crawlers read sitemap URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | T | sitemap.xml URLs | accept | URLs are hardcoded constants in build script, not user-provided. Low risk |
| T-04-04 | I | robots.txt sitemap URL | accept | Static file with hardcoded URL. No dynamic injection |
</threat_model>

<verification>
After Plan 02 completes:
1. `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8'))"` exits 0
2. `grep '"seo"' src/i18n/locales/en.json` returns non-zero
3. `grep '"seo"' src/i18n/locales/es.json` returns non-zero
4. `grep '"seo"' src/i18n/locales/de.json` returns non-zero
5. `grep 'humbertobellor.github.io/dossier/sitemap.xml' public/robots.txt` returns non-zero
6. After build: `grep -c 'hreflang' dist/sitemap.xml` returns >= 9 (3 locales × 3 hreflang each)
7. After build: `grep 'x-default' dist/sitemap.xml` returns non-zero
</verification>

<success_criteria>
- All 3 locale JSON files have seo.* keys with locale-specific meta content
- robots.txt updated with new sitemap URL
- Build generates dist/sitemap.xml with hreflang for all 3 locales + x-default
- Sitemap uses correct XML namespaces
- All URLs in sitemap point to humbertobellor.github.io/dossier/
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-i18n/04-02-SUMMARY.md`
</output>
