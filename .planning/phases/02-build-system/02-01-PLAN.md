---
phase: 02-build-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/html/_head.html (NEW)
  - src/html/_nav.html (NEW)
  - src/html/_footer.html (NEW)
  - src/html/hero.html (NEW)
  - src/html/experience.html (NEW)
  - src/html/skills.html (NEW)
  - src/html/clients.html (NEW)
  - src/html/cta.html (NEW)
  - src/html/footer.html (NEW)
  - src/html/index.html (NEW)
  - src/html/404.html (NEW)
autonomous: true
requirements:
  - STC-01    # Build script assembles single index.html from HTML partials — this plan creates the partials
must_haves:
  truths:
    - "All 7 content sections (hero, experience, skills, clients, CTA, footer) exist as separate HTML partial files"
    - "Shared wrapper partials (_head.html, _nav.html, _footer.html) exist for template inclusion"
    - "Main template (index.html) uses marker comments for partial inclusion"
    - "404 template (404.html) uses same shared wrapper markers with different body content"
    - "All content matches the current React SPA output — no content loss, no text drift"
  artifacts:
    - path: "src/html/_head.html"
      provides: "HTML head content — meta tags, SEO, OG, JSON-LD, font preloads, CSS+script blocks"
      min_lines: 25
      contains: "<meta charset"
    - path: "src/html/_nav.html"
      provides: "Sticky navigation bar with wk- classes"
      contains: "wk-nav"
    - path: "src/html/_footer.html"
      provides: "Footer closing content — language switcher, links"
      contains: "wk-footer"
    - path: "src/html/hero.html"
      provides: "Hero section — headshot, title, stats, CTA buttons"
      contains: "wk-hero"
    - path: "src/html/experience.html"
      provides: "Experience section — job timeline cards"
      contains: "wk-section-experience"
    - path: "src/html/skills.html"
      provides: "Skills section — grid of skill cards with icons"
      contains: "wk-section-skills"
    - path: "src/html/clients.html"
      provides: "Client/industry showcase grid"
      contains: "wk-section-clients"
    - path: "src/html/cta.html"
      provides: "CTA section — call to action with buttons"
      contains: "wk-cta-section"
    - path: "src/html/footer.html"
      provides: "Footer content section — links, copyright"
      contains: "wk-footer"
    - path: "src/html/index.html"
      provides: "Main template with include markers"
      contains: "<!--#include"
    - path: "src/html/404.html"
      provides: "404 template with include markers"
      contains: "<!--#include"
  key_links:
    - from: "src/html/index.html"
      to: "src/html/*.html"
      via: "<!--#include path/to/partial.html--> markers"
      pattern: "<!--#include"
    - from: "src/html/*.html"
      to: "src/styles/*.css"
      via: "Class references (wk-* naming convention)"
      pattern: "wk-"
---

<objective>
Create all HTML partial files and templates for Phase 2's build system. This plan produces the content layer — 9 section/shared partials, 1 main template, and 1 404 template — all using the marker replacement pattern (D-05).

Purpose: Extract all page content from the existing React components into standalone HTML partials that the build script (Plan 02) will assemble. After this plan, every visual section of the dossier exists as a hand-authored HTML fragment ready for assembly.

Output: 11 HTML files in `src/html/` — 9 partials, 1 main template, 1 404 template
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-system/02-CONTEXT.md

<interfaces>
**Phase 1 CSS class reference (from wolknitive-base.css):**
- Container: `.wk-container` (max-width: 72rem, margin: 0 auto, padding-inline: 1.5rem)
- Sections: `.wk-section` (padding: 5rem 1.5rem, 6rem at 768px), `.wk-bg-sunken`, `.wk-bg-default`, `.wk-bg-elevated`
- Grids: `.wk-grid-skills`, `.wk-grid-experience` (1→2→3 col at 768→1024px), `.wk-grid-clients` (2→3 col at 768px)
- Cards: `.wk-card`, `.wk-card-skills`, `.wk-card-experience`, `.wk-card-client` (all with hover states)
- Nav: `.wk-nav`, `.wk-nav-inner`, `.wk-nav-links`, `.wk-nav-link`, `.wk-nav-toggle`, `.wk-btn-text-desktop`
- Buttons: `.wk-btn-cta`, `.wk-btn-outline` (both with hover states)
- Tags: `.wk-tag`, `.wk-tag-accent`
- Hero: `.wk-hero`, `.wk-hero-inner`, `.wk-hero-content`, `.wk-hero-photo`, `.wk-hero-panel`, `.wk-stat-grid`, `.wk-stat-card`
- Footer: `.wk-footer`
- Typography: `.wk-section-header`, `.wk-section-heading`, `.wk-section-subtitle`, `.wk-label`, `.wk-eyebrow`, `.wk-rule`
- Animations: `.wk-reveal`, `data-delay`, `.wk-anim-fade-*`, `.wk-anim-bounce-y`
- Icons: `.wk-icon-sm` (16px), `.wk-icon-md` (20px), `.wk-icon-lg` (24px)

**Marker syntax (per D-05):**
```html
<!--#include path/to/partial.html-->
```

**Inline SVG path data:** Available in `node_modules/lucide-static/lib/icons/` within the monorepo. Each icon has `{name}.js` exporting an SVG path string.

**Source content (from current React SPA):**
- `src/pages/home.tsx` — All 1279 lines of content to extract into partials
- `src/pages/not-found.tsx` — 404 content
- `index.html` — head content (meta tags, OG, JSON-LD, font preloads)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: create shared wrapper partials (_head.html, _nav.html, _footer.html)</name>
  <files>
    src/html/_head.html (NEW),
    src/html/_nav.html (NEW),
    src/html/_footer.html (NEW)
  </files>
  <read_first>
    src/pages/home.tsx (entire file),
    index.html (entire file — source for head content),
    .planning/phases/02-build-system/02-CONTEXT.md (D-01 through D-04, D-09, D-12 through D-15)
  </read_first>
  <action>
    Create `src/html/` directory (per D-02) and three shared/wrapper partials.

    **1a. `src/html/_head.html` — HTML head with SEO, meta tags, JSON-LD, font preloads, CSS blocks, and scripts.**

    This partial provides the entire `<head>` content including:
    - `<meta charset="UTF-8">`, `<meta name="viewport">`, `<title>`, `<meta name="description">`, `<meta name="keywords">`, `<meta name="robots">`, `<meta name="author">`
    - Canonical URL: `<link rel="canonical" href="https://humbertobello.replit.app/">` (Phase 4 will update to `/dossier/`)
    - Open Graph and Twitter Card meta tags — copy EXACTLY from `index.html` lines 15-32
    - JSON-LD structured data — copy EXACTLY from `index.html` lines 34-82
    - Font preload `<link>` tags — copy EXACTLY from `index.html` lines 87-96

    CSS placeholder blocks (per D-09):
    ```html
    <style id="wk-tokens">/* injected by build script */</style>
    <style id="wk-base">/* injected by build script */</style>
    <style id="wk-animations">/* injected by build script */</style>
    ```

    i18next vendor script tags (placeholder — will be populated in Plan 03):
    ```html
    <script src="/locales/i18next.min.js"></script>
    <script src="/locales/i18nextBrowserLanguageDetector.min.js"></script>
    ```

    CSP meta tag placeholder (populated in Plan 03):
    ```html
    <meta http-equiv="Content-Security-Policy" content="">
    ```

    IntersectionObserver inline `<script>` — copy from Phase 1's work (it's in the current `index.html`). If the current `index.html` has the IntersectionObserver script (from Phase 1), include it here. The script should have both the reveal observer and nav section tracking logic.

    **1b. `src/html/_nav.html` — Sticky navigation bar.**

    Reference: `src/pages/home.tsx` sections:
    - Sticky nav structure (around lines 232-320): `<nav>` with background/border, flex layout, nav links for each section
    - Logo/name on left: `<span class="wk-nav-brand">Humberto Bello</span>` (or similar)
    - Nav links as `<button class="wk-nav-link" data-section="hero|experience|skills|clients|cta">SectionName</button>`
    - Mobile nav toggle placeholder: `<button class="wk-nav-toggle" aria-label="Toggle navigation">...</button>` (hamburger icon as inline SVG)
    - Language switcher placeholder: `<div class="wk-lang-switch"><button class="wk-nav-link" data-i18n-switch="en">EN</button><button class="wk-nav-link" data-i18n-switch="es">ES</button><button class="wk-nav-link" data-i18n-switch="de">DE</button></div>` (per D-14)

    Use these CSS classes: `wk-nav`, `wk-nav-inner`, `wk-nav-links`, `wk-nav-link`, `wk-nav-toggle`
    The active section tracking is handled by the IntersectionObserver in _head.html.

    **1c. `src/html/_footer.html` — Closing wrapper content.**

    Include:
    - The `<script>` block for i18next init (placeholder — populated in Plan 03)
    - Closing `</body></html>`

    ```html
    <script>
    <!-- i18next init — populated in Plan 03 -->
    </script>
    </body>
    </html>
    ```
  </action>
  <acceptance_criteria>
    - `src/html/` directory exists
    - `src/html/_head.html` exists with `<meta charset="UTF-8">`, `<style id="wk-tokens">`, `<script>...IntersectionObserver...</script>`, JSON-LD block, font preloads
    - `src/html/_nav.html` exists with `class="wk-nav"`, `wk-nav-link` buttons, `data-i18n-switch` attributes
    - `src/html/_footer.html` exists with `</body></html>`
    - All content in `_head.html` matches `index.html` for SEO/OG/JSON-LD/font preloads
  </acceptance_criteria>
  <verify>
    <automated>test -f src/html/_head.html && test -f src/html/_nav.html && test -f src/html/_footer.html && grep -c "wk-nav" src/html/_nav.html && grep -c "wk-tokens" src/html/_head.html</automated>
  </verify>
  <done>
    _head.html, _nav.html, _footer.html created with all head content, nav structure, and footer close.
  </done>
</task>

<task type="auto" tdd="false">
  <name>task 2: create content section partials (hero, experience, skills, clients, cta, footer)</name>
  <files>
    src/html/hero.html (NEW),
    src/html/experience.html (NEW),
    src/html/skills.html (NEW),
    src/html/clients.html (NEW),
    src/html/cta.html (NEW),
    src/html/footer.html (NEW)
  </files>
  <read_first>
    src/pages/home.tsx (entire file — 1279 lines of HTML content to extract),
    .planning/phases/01-css-architecture/01-UI-SPEC.md (section layout specs)
  </read_first>
  <action>
    Extract HTML content from `src/pages/home.tsx` into 6 section partials. Each partial should be wrapped in a `<section>` tag with appropriate `id`, `class`, and `data-wk-reveal` attributes.

    **CRITICAL RULES for JSX→HTML conversion:**
    - Replace all `{condition && <JSX/>}` with the TRUE branch rendered directly in HTML. For example, where React conditionally renders the scroll hint chevron, always render it.
    - Replace all `{items.map(item => <JSX/>)}` with explicit HTML for ALL items. For example, skill cards loop → render all ~18 skill cards as hardcoded HTML. The data is static (no API) — hardcoding is correct.
    - Replace `{t('key')}` i18n calls with English text directly (Phase 4 adds multi-locale). For example `{t('hero.title')}` → `Humberto Bello · AI Architect & Engineering Leader`.
    - Replace `<IconName size={N} />` with inline `<svg>` elements using lucide's SVG paths. Use `stroke="currentColor"` and appropriate `wk-icon-*` class for size.
    - Replace all `onMouseEnter`/`onMouseLeave` handlers with nothing (CSS `:hover` in base.css handles it).
    - Replace `style={{}}` inline styles with nothing (all styling is in CSS classes from Phase 1).
    - Keep `data-wk-reveal`, `data-delay`, and `data-section` attributes — these drive vanilla JS behavior.

    **2a. `src/html/hero.html`:**
    Extract from the Hero section (around lines 408-620 of home.tsx):
    - The decorative background panel (`wk-hero-panel`)
    - Content column: title (`wk-section-heading`), subtitle, stat grid (`wk-stat-grid` with MapPin icon + location, years experience, etc.)
    - Both CTA buttons: "Let's Talk" (`wk-btn-cta`) and "View LinkedIn" (`wk-btn-outline`)
    - Photo column: `<picture>` element with AVIF (srcset) and WebP fallback for the headshot
    - Animation classes: `wk-anim-fade-left`, `wk-anim-fade-right`, `wk-anim-fade-scale`, `wk-anim-fade-up`, `wk-anim-fade-up-sm`

    Hero image paths (from CONTEXT.md D-11):
    - `<source srcset="/images/headshot-corp_1776959044728.avif" type="image/avif">`
    - `<img src="/images/headshot-corp_1776959044728.webp" alt="Humberto Bello" ...>`

    **2b. `src/html/experience.html`:**
    Extract from Experience section (around lines 620-850 of home.tsx):
    - Section header: eyebrow ("BACKGROUND"), heading ("Engineering & Strategy"), subtitle
    - Double rule (`.wk-rule`)
    - Experience grid (`.wk-grid-experience`) with job cards for each role
    - Each job card (`.wk-card-experience`): company name, title, date range, tag badges (`.wk-tag`, `.wk-tag-accent`)
    - Add `data-wk-reveal` on each card with staggered delays (`data-delay="0.07s"`, `data-delay="0.14s"`, etc.)

    **2c. `src/html/skills.html`:**
    Extract from Skills section (around lines 850-980 of home.tsx):
    - Section header: eyebrow ("EXPERTISE"), heading ("Building for Impact"), subtitle
    - Skills grid (`.wk-grid-skills`) with skill cards
    - Each skill card (`.wk-card.wk-card-skills`): icon (inline SVG), title, list of skill items
    - Skills from home.tsx include at least: Agentic AI (Brain icon), RAG Systems (Cloud icon), Cloud Architecture (Shield icon), Engineering Leadership (Users icon), etc.
    - Each card must have `data-wk-reveal` with staggered delays

    **2d. `src/html/clients.html`:**
    Extract from Clients section (around lines 980-1150 of home.tsx):
    - Section header: eyebrow ("INDUSTRY"), heading ("Sectors & Impact"), subtitle
    - Client grid (`.wk-grid-clients`) with client cards for each sector
    - Each client card (`.wk-card-client`): inline SVG icon (24px), sector name, category label
    - Client sectors from home.tsx are roughly: Financial Services, Banking & Capital Markets, Fintech, Telecommunications, HealthTech, Retail & Commerce

    **2e. `src/html/cta.html`:**
    Extract from CTA section (around lines 1150-1220 of home.tsx):
    - Background variant: `.wk-section.wk-bg-sunken`
    - Section heading: larger version, call to action
    - Two buttons: primary CTA and outline button
    - `data-wk-reveal` on the section

    **2f. `src/html/footer.html`:**
    Extract from Footer section (around lines 1220-1279 of home.tsx):
    - `<footer class="wk-footer">` inside `.wk-container`
    - Copyright/name on left
    - Links to LinkedIn, GitHub, Email

    For ALL inline SVGs (lucide icons), source the exact SVG paths from `node_modules/lucide-static/lib/icons/`. Use the pattern:
    ```html
    <svg class="wk-icon-sm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="...lucide path data..."/>
    </svg>
    ```
  </action>
  <acceptance_criteria>
    - All 6 section partials exist in `src/html/`
    - `hero.html` contains `<picture>`, wk-hero classes, CTA buttons, headshot image references
    - `experience.html` contains `wk-grid-experience` with multiple job cards, `.wk-tag` badges
    - `skills.html` contains `wk-grid-skills` with multiple skill cards, inline SVG icons
    - `clients.html` contains `wk-grid-clients` with client sector cards
    - `cta.html` contains `wk-bg-sunken`, CTA buttons
    - `footer.html` contains `wk-footer`, LinkedIn/GitHub links
    - Every `{t('...')}` call is replaced with raw English text
    - Every `<IconName>` JSX element is replaced with inline `<svg>`
    - No JSX syntax remains (no `className`, no `{...}` expressions)
    - `data-wk-reveal` and `data-delay` attributes present on animated elements
  </acceptance_criteria>
  <verify>
    <automated>for f in hero experience skills clients cta footer; do test -f "src/html/${f}.html" || echo "MISSING: ${f}.html"; done && grep -c "wk-hero" src/html/hero.html && grep -c "wk-grid" src/html/experience.html && grep -c "wk-grid" src/html/skills.html && grep -c "wk-grid" src/html/clients.html && grep -c "wk-bg-sunken" src/html/cta.html && grep -c "wk-footer" src/html/footer.html</automated>
  </verify>
  <done>
    All 6 section partials created with English content extracted from home.tsx. Inline SVGs replace lucide icons. JSX converted to HTML.
  </done>
</task>

<task type="auto" tdd="false">
  <name>task 3: create main template (index.html) and 404 template (404.html) with include markers</name>
  <files>
    src/html/index.html (NEW),
    src/html/404.html (NEW)
  </files>
  <read_first>
    src/pages/not-found.tsx (404 content),
    .planning/phases/02-build-system/02-CONTEXT.md (D-05 marker pattern, D-16 for 404 generation)
  </read_first>
  <action>
    Create two template files that use `<!--#include-->` markers for assembly by the build script.

    **3a. `src/html/index.html` — Main template:**

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
    <!--#include src/html/_head.html-->
    </head>
    <body>
    <!--#include src/html/_nav.html-->

    <main>
    <!--#include src/html/hero.html-->
    <!--#include src/html/experience.html-->
    <!--#include src/html/skills.html-->
    <!--#include src/html/clients.html-->
    <!--#include src/html/cta.html-->
    <!--#include src/html/footer.html-->
    </main>

    <!--#include src/html/_footer.html-->
    ```

    **3b. `src/html/404.html` — 404 template:**

    Same layout as index.html but with one section partial replaced by 404 content:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
    <!--#include src/html/_head.html-->
    </head>
    <body>
    <!--#include src/html/_nav.html-->

    <main>
    <section class="wk-section" style="min-height: 60vh; display: flex; align-items: center; justify-content: center;">
      <div class="wk-container" style="text-align: center;">
        <h1 class="wk-section-heading">Page Not Found</h1>
        <p class="wk-section-subtitle" style="margin-bottom: 2rem;">The page you're looking for doesn't exist.</p>
        <a href="/" class="wk-btn-cta">Back to Home</a>
      </div>
    </section>
    </main>

    <!--#include src/html/_footer.html-->
    ```

    Both templates must have exactly 16 `<!--#include-->` markers total (index.html) and 13 markers (404.html). Count them to verify.

    The marker syntax is (per D-05): `<!--#include path/to/partial.html-->`
    Use RELATIVE paths from the project root (e.g., `src/html/hero.html` not `hero.html`). The build script (Plan 02) reads from the project root and replaces markers.
  </action>
  <acceptance_criteria>
    - `src/html/index.html` exists with exactly 9 `<!--#include` markers
    - `src/html/404.html` exists with exactly 5 `<!--#include` markers (shared wrappers only)
    - `index.html` references all section partials: hero, experience, skills, clients, cta, footer
    - `index.html` references all shared partials: _head, _nav, _footer
    - `404.html` uses same `_head.html`, `_nav.html`, `_footer.html` wrappers
    - `404.html` has inline 404 content between nav and footer
    - No hardcoded HTML content in templates — only markers (except 404 body content)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "<!--#include" src/html/index.html && grep -c "<!--#include" src/html/404.html && grep -c "Page Not Found" src/html/404.html</automated>
  </verify>
  <done>
    Main template and 404 template created with include markers. Content assembled by build script in Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template markers → Build script | Include markers processed by build script, no user input reaches here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | `<!--#include-->` markers in templates | mitigate | Markers reference fixed local paths only. No dynamic paths, no URL-based includes, no user-controlled input. |
| T-02-02 | S (Spoofing) | Inline SVG icon paths | accept | SVG paths are extracted from lucide-static package, a controlled dependency. No user-provided SVG content. |
</threat_model>

<verification>
1. All 11 files exist in `src/html/`
2. Each section partial renders as a valid HTML fragment (check basic structure)
3. index.html has correct marker count (9 includes)
4. 404.html has correct marker count (5 includes)
5. No JSX syntax (`className`, `{#each}`, `{t()}`) remains in any partial
6. English text is hardcoded — no i18n function calls
7. Responsive breakpoints, animation classes, and data-* attributes are preserved
</verification>

<success_criteria>
- [ ] `src/html/` directory created with all 11 files
- [ ] _head.html, _nav.html, _footer.html shared partials created
- [ ] hero.html, experience.html, skills.html, clients.html, cta.html, footer.html section partials created
- [ ] index.html main template with include markers created
- [ ] 404.html with include markers + inline 404 content created
- [ ] All JSX→HTML conversion rules applied (no className, no t(), no IconName, no event handlers)
- [ ] Content matches current React SPA visually
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-system/02-01-SUMMARY.md`
</output>
