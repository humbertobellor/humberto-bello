---
phase: 01-css-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/wolknitive-tokens.css
  - src/styles/wolknitive-base.css (NEW)
  - src/styles/wolknitive-animations.css (NEW)
  - src/index.css
  - index.html
autonomous: true
requirements:
  - QLT-01    # Duplicate color constants consolidated to single CSS source
  - VIZ-05    # CSS-only hero entrance animations preserved
  - VIZ-06    # Wolknitive design tokens preserved
  - INF-06    # Print-friendly CSS stylesheet
  - STC-02    # All CSS is hand-authored using Wolknitive custom properties

must_haves:
  truths:
    - "All Wolknitive tokens (colors, fonts, spacing, radii, shadows) exist in one CSS source"
    - "No Tailwind directives exist in CSS source files"
    - "All 7 hero entrance @keyframes animations are in animations.css with working animation classes"
    - "IntersectionObserver script exists in index.html to support scroll-triggered animations"
    - "Print stylesheet exists that hides nav, CTA, hero panel and collapses grids"
    - "Responsive breakpoints (768px, 1024px) are defined as CSS media queries"
  artifacts:
    - path: "src/styles/wolknitive-tokens.css"
      provides: "Single source of truth for all design tokens"
      contains: "--space-xs, --space-sm, --space-base, etc."
    - path: "src/styles/wolknitive-base.css"
      provides: "Section layouts, card patterns, nav, buttons, tags, grid classes, responsive breakpoints, print"
      min_lines: 200
    - path: "src/styles/wolknitive-animations.css"
      provides: "All @keyframes definitions and animation classes"
      contains: "@keyframes wkFadeSlideLeft"
    - path: "src/index.css"
      provides: "No Tailwind imports or directives"
      not_contains: "@import \"tailwindcss\""
    - path: "index.html"
      provides: "IntersectionObserver inline script for scroll-triggered animations"
      contains: "IntersectionObserver"
    - path: "src/styles/wolknitive-base.css"
      provides: "Print stylesheet"
      contains: "@media print"
  key_links:
    - from: "src/styles/wolknitive-tokens.css"
      to: "src/styles/wolknitive-base.css"
      via: "CSS custom properties consumed by all classes"
      pattern: "var\\(--"
    - from: "index.html"
      to: "*.wk-reveal"
      via: "IntersectionObserver adds .is-visible class"
      pattern: "IntersectionObserver"
    - from: "src/styles/wolknitive-base.css"
      to: "@media (min-width: 768px|1024px)"
      via: "Responsive breakpoints"
      pattern: "@media"
---

<objective>
Create the CSS file infrastructure for Phase 1: consolidate Wolknitive tokens in a single source, create hand-authored base stylesheet with section layouts/responsive breakpoints/print styles, move hero animations to dedicated file, and add IntersectionObserver inline script.

Purpose: Establish the CSS architecture that Plan 02's source code transformation will consume. After this plan, all 7 content sections should render identically when running `npm run dev` — same visual appearance, same responsive behavior, same animations.

Output: wolknitive-tokens.css (consolidated), wolknitive-base.css (NEW), wolknitive-animations.css (NEW), index.css (cleaned), index.html (+IntersectionObserver)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-architecture/01-CONTEXT.md
@.planning/phases/01-css-architecture/01-UI-SPEC.md
@.planning/phases/01-css-architecture/01-RESEARCH.md
@.planning/phases/01-css-architecture/01-PATTERNS.md
@.planning/phases/01-css-architecture/01-VALIDATION.md

<interfaces>
**Existing CSS custom properties in wolknitive-tokens.css:**
- Color: --vellum-50/100/200/300/400/500/600/700/800, --ink, --teal-50/100/200/300/400/500/600/700, --plum-50/100/200/300/400/500/600/700, --amber-100/300/500, --moss-500/100, --brick-500/100
- Semantic: --bg, --bg-elevated, --bg-sunken, --bg-inverse, --fg-1/2/3/4, --fg-on-dark, --accent, --accent-hover, --accent-soft, --accent-2, --accent-2-soft, --highlight, --highlight-soft
- Borders: --rule-strong, --rule, --rule-soft
- Radii: --radius-xs/sm/md/lg/pill
- Shadows: --shadow-1/2/3, --shadow-inset
- Fonts: --font-display, --font-body, --font-ui, --font-mono
- Type scale: --fs-xs/sm/base/md/lg/xl/2xl/3xl/4xl/5xl
- Line height: --lh-tight/snug/normal/loose
- Existing classes: .wk-rule, .wk-label, .wk-eyebrow

**TS Color Constants (to be removed in Plan 02, listed here for reference):**
- INK=#14110B, TEAL=#1B4E4A, TEAL_6=#103A37, V50=#FAF6EC, V100=#F3ECD9, V200=#E7DCC0, V300=#CFBE96, V400=#9C8A64, V500=#6B5C3E, V700=#2E261A

**index.html Vite entry:**
- Contains SEO meta tags, OG tags, canonical URL, font preloads, `<div id="root">`
- Script at bottom loads Vite's entry module
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: add spacing tokens and line-height tokens to wolknitive-tokens.css</name>
  <files>src/styles/wolknitive-tokens.css</files>
  <read_first>src/styles/wolknitive-tokens.css (entire file — 187 lines)</read_first>
  <action>
    After the `--lh-loose: 1.7;` line inside `:root` (line 156), insert the following spacing and line-height tokens in the appropriate section:

    ```
      /* Spacing scale (from UI-SPEC spacing contract) */
      --space-2xs:  4px;
      --space-xs:   6px;
      --space-sm:   8px;
      --space-md:   12px;
      --space-base: 16px;
      --space-lg:   24px;
      --space-xl:   32px;
      --space-2xl:  48px;
      --space-3xl:  80px;
      --space-4xl:  112px;
      --gap-grid:   20px;
    ```

    Do NOT modify any existing tokens. Do NOT remove or alter @font-face declarations. Do NOT change or add other content.
  </action>
  <acceptance_criteria>
    - `src/styles/wolknitive-tokens.css` contains `--space-xs:  6px;` and `--space-4xl:  112px;` inside `:root { }`
    - `grep -c "space-2xs" src/styles/wolknitive-tokens.css` returns >= 1
    - All original tokens remain intact
    - `npm run typecheck` passes
    - `npm run dev` starts without CSS errors
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\-\-space-4xl" src/styles/wolknitive-tokens.css || echo "FAIL: spacing tokens not found"</automated>
  </verify>
  <done>
    Spacing scale tokens (--space-xs through --space-4xl) are defined in wolknitive-tokens.css. Type check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>task 2: create wolknitive-base.css with section layouts, responsive breakpoints, and print stylesheet</name>
  <files>src/styles/wolknitive-base.css</files>
  <read_first>src/styles/wolknitive-tokens.css, src/index.css, src/pages/home.tsx (lines 230-1279 for section structure reference), .planning/phases/01-css-architecture/01-UI-SPEC.md (layout, spacing, colors sections)</read_first>
  <action>
    Create `src/styles/wolknitive-base.css` with the following classes and rules. Use EXACT values from UI-SPEC.md and CONTEXT.md — do not invent sizes, colors, or spacing.

    **1. CSS Reset and Base:**
    ```css
    *, *::before, *::after { box-sizing: border-box; }
    body {
      font-family: var(--font-body);
      background: var(--bg);
      color: var(--fg-1);
      -webkit-font-smoothing: antialiased;
      text-rendering: optimizeLegibility;
      margin: 0;
    }
    ```

    **2. Container (D-11):**
    ```css
    .wk-container {
      max-width: 72rem;
      margin: 0 auto;
      padding-inline: 1.5rem;
    }
    ```

    **3. Section Layout:**
    ```css
    .wk-section {
      padding: 5rem 1.5rem;
    }
    @media (min-width: 768px) {
      .wk-section { padding: 6rem 1.5rem; }
    }
    ```

    **4. Section Header (eyebrow + heading + subtitle — pattern from UI-SPEC typography contract):**
    ```css
    .wk-section-header { margin-bottom: 3rem; }
    .wk-section-heading {
      font-family: var(--font-display);
      font-weight: 500;
      font-size: clamp(1.9rem, 4vw, 2.8rem);
      line-height: 1.15;
      color: var(--fg-1);
      margin: 0.5rem 0 0;
    }
    .wk-section-subtitle {
      font-family: var(--font-body);
      font-size: var(--fs-md, 18px);
      line-height: 1.6;
      color: var(--fg-3);
      margin-top: 0.75rem;
    }
    ```

    **5. Grid Classes (D-10, from UI-SPEC grid layout table — EXACT breakpoints and column counts):**
    ```css
    .wk-grid-skills, .wk-grid-experience {
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--gap-grid, 20px);
    }
    @media (min-width: 768px) {
      .wk-grid-skills, .wk-grid-experience {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    @media (min-width: 1024px) {
      .wk-grid-skills, .wk-grid-experience {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    .wk-grid-clients {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
      margin-bottom: 4rem;
    }
    @media (min-width: 768px) {
      .wk-grid-clients {
        grid-template-columns: repeat(3, 1fr);
        gap: 1rem;
      }
    }
    ```

    **6. Card Classes (from repeated inline style pattern in home.tsx):**
    ```css
    .wk-card {
      background: var(--vellum-100);
      border: 1px solid var(--vellum-200);
      border-radius: var(--radius-lg, 10px);
      padding: 1.375rem 1.5rem;
      box-shadow: var(--shadow-1);
      transition: box-shadow 0.2s, background-color 0.2s;
    }
    .wk-card:hover {
      box-shadow: var(--shadow-2);
    }
    .wk-card-skills {
      background: var(--bg-elevated);
    }
    .wk-card-experience {
      background: var(--bg-elevated);
    }
    .wk-card-client {
      background: var(--bg);
      border-color: var(--rule-soft);
      text-align: center;
      padding: 1.5rem;
      cursor: default;
    }
    .wk-card-client:hover {
      background: var(--vellum-100);
    }
    ```

    **7. Nav Classes (from nav patterns in home.tsx lines 232-407):**
    ```css
    .wk-nav {
      position: sticky;
      top: 0;
      z-index: 50;
      background: rgba(250, 246, 236, 0.92);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      border-bottom: 1px solid var(--rule-soft);
    }
    .wk-nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      max-width: 72rem;
      margin: 0 auto;
      padding: 0.75rem 1.5rem;
    }
    .wk-nav-links {
      display: none;
      gap: 0.25rem;
    }
    @media (min-width: 768px) {
      .wk-nav-links { display: flex; }
    }
    .wk-nav-link {
      font-family: var(--font-ui);
      font-size: var(--fs-sm, 14px);
      font-weight: 500;
      color: var(--fg-3);
      padding: 0.375rem 0.75rem;
      border-radius: var(--radius-sm, 4px);
      cursor: pointer;
      transition: color 0.2s, background-color 0.2s;
      border: none;
      background: transparent;
    }
    .wk-nav-link:hover {
      color: var(--accent);
      background: var(--accent-soft);
    }
    .wk-nav-link.active {
      color: var(--accent);
    }
    ```

    **8. Button Classes:**
    ```css
    .wk-btn-cta {
      font-family: var(--font-ui);
      font-size: var(--fs-sm, 14px);
      font-weight: 600;
      padding: 0.625rem 1.375rem;
      border-radius: var(--radius-sm, 4px);
      background: var(--accent);
      color: var(--vellum-50);
      border: none;
      cursor: pointer;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: background-color 0.2s, transform 0.1s;
    }
    .wk-btn-cta:hover {
      background: var(--accent-hover);
    }
    .wk-btn-outline {
      font-family: var(--font-ui);
      font-size: var(--fs-sm, 14px);
      font-weight: 500;
      padding: 0.625rem 1.375rem;
      border-radius: var(--radius-sm, 4px);
      background: transparent;
      color: var(--fg-1);
      border: 1px solid var(--rule);
      cursor: pointer;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: background-color 0.2s, border-color 0.2s;
    }
    .wk-btn-outline:hover {
      background: var(--vellum-100);
      border-color: var(--fg-3);
    }
    ```

    **9. Tag Pill Classes:**
    ```css
    .wk-tag {
      font-family: var(--font-ui);
      font-size: var(--fs-xs, 12px);
      font-weight: 500;
      padding: 0.25rem 0.5625rem;
      border-radius: var(--radius-pill, 999px);
      background: var(--vellum-200);
      color: var(--fg-2);
      border: none;
      display: inline-block;
      line-height: 1.4;
    }
    .wk-tag-accent {
      background: var(--accent-soft);
      color: var(--accent);
    }
    ```

    **10. Footer Classes:**
    ```css
    .wk-footer {
      text-align: center;
      padding: 3rem 1.5rem;
      border-top: 1px solid var(--rule-soft);
    }
    @media (min-width: 768px) {
      .wk-footer {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        text-align: left;
      }
    }
    ```

    **11. Hero Layout Classes (from home.tsx hero section):**
    ```css
    .wk-hero {
      position: relative;
      padding: 3rem 0;
      overflow: hidden;
    }
    @media (min-width: 768px) {
      .wk-hero { padding: 5rem 0; }
    }
    .wk-hero-inner {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }
    @media (min-width: 768px) {
      .wk-hero-inner {
        flex-direction: row;
        align-items: center;
      }
    }
    .wk-hero-content {
      width: 100%;
    }
    @media (min-width: 768px) {
      .wk-hero-content {
        width: 50%;
        margin-left: auto;
        padding: 0 3rem 0 0;
      }
    }
    .wk-hero-photo {
      width: 100%;
    }
    @media (min-width: 768px) {
      .wk-hero-photo {
        width: 50%;
      }
    }
    .wk-hero-panel {
      display: none;
    }
    @media (min-width: 768px) {
      .wk-hero-panel {
        display: block;
        position: absolute;
        inset: 0;
        left: 0;
        width: 50%;
      }
    }
    .wk-stat-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
      margin-top: 1.5rem;
    }
    .wk-stat-card {
      font-family: var(--font-ui);
      font-size: var(--fs-sm, 14px);
      color: var(--fg-3);
      padding: 0.75rem;
      border-radius: var(--radius-md, 6px);
      background: var(--bg-elevated);
    }
    ```

    **12. Icon Size Classes (D-23):**
    ```css
    .wk-icon-sm { width: 16px; height: 16px; }
    .wk-icon-md { width: 20px; height: 20px; }
    .wk-icon-lg { width: 24px; height: 24px; }
    svg.wk-icon-sm, svg.wk-icon-md, svg.wk-icon-lg {
      display: inline-block;
      vertical-align: middle;
      flex-shrink: 0;
    }
    ```

    **13. Section Background Variants (from UI-SPEC section padding table):**
    ```css
    .wk-bg-sunken { background: var(--bg-sunken); }
    .wk-bg-default { background: var(--bg); }
    .wk-bg-elevated { background: var(--bg-elevated); }
    ```

    **14. Mobile Nav Toggle (from home.tsx mobile nav pattern):**
    ```css
    .wk-nav-toggle {
      display: flex;
      align-items: center;
    }
    @media (min-width: 768px) {
      .wk-nav-toggle { display: none; }
    }
    .wk-btn-text-desktop { display: none; }
    @media (min-width: 768px) {
      .wk-btn-text-desktop { display: inline; }
    }
    .wk-mobile-hidden {
      display: none;
    }
    @media (min-width: 768px) {
      .wk-mobile-hidden { display: block; }
    }
    ```

    **15. Print Stylesheet (D-12 through D-15):**
    ```css
    @media print {
      .wk-nav,
      .wk-cta-section,
      .wk-scroll-hint,
      .wk-hero-panel { display: none !important; }

      .wk-grid-skills,
      .wk-grid-experience,
      .wk-grid-clients {
        grid-template-columns: 1fr !important;
      }

      .wk-section {
        break-inside: avoid;
        page-break-inside: avoid;
        padding: 1rem 0 !important;
      }

      .wk-card {
        box-shadow: none !important;
        border: 1px solid #ccc !important;
      }

      .wk-card-client {
        page-break-inside: avoid;
      }

      body {
        background: white !important;
        color: black !important;
      }

      .wk-hero { padding: 0 !important; }

      .wk-hero-inner {
        flex-direction: row !important;
      }

      .wk-stat-card {
        background: #f5f5f5 !important;
      }

      .wk-btn-cta,
      .wk-btn-outline {
        border: 1px solid #999 !important;
      }

      a { color: var(--accent) !important; }
    }
    ```

    After creating the file, verify it has correct CSS syntax.
  </action>
  <acceptance_criteria>
    - `src/styles/wolknitive-base.css` exists and is >= 200 lines
    - Contains `.wk-container` with `max-width: 72rem`
    - Contains `.wk-grid-experience` with `grid-template-columns: 1fr`
    - Contains `@media print` block
    - Contains `.wk-nav` class
    - Contains `.wk-btn-cta` with `background: var(--accent)`
    - Contains `.wk-tag` class
    - Contains `.wk-hero-content` class
    - All CSS values match the UI-SPEC spacing/color/typography contracts
    - `npm run typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>test -f src/styles/wolknitive-base.css && wc -l src/styles/wolknitive-base.css && grep -c "@media print" src/styles/wolknitive-base.css</automated>
  </verify>
  <done>
    wolknitive-base.css created with all section layout, card, nav, button, tag, grid, hero, footer, responsive, icon, and print classes. Render-ready for Plan 02 consumption.
  </done>
</task>

<task type="auto" tdd="false">
  <name>task 3: create animations.css, add IntersectionObserver script, clean index.css</name>
  <files>
    src/styles/wolknitive-animations.css,
    index.html,
    src/index.css
  </files>
  <read_first>src/index.css (entire file — 88 lines), index.html (entire file), src/components/FadeIn.tsx, .planning/phases/01-css-architecture/01-RESEARCH.md (IntersectionObserver section)</read_first>
  <action>
    **3a. Create `src/styles/wolknitive-animations.css`:**

    Move all @keyframes and animation classes from `src/index.css` lines 52-88 into this new file. Then add the following scroll-triggered animation classes:

    ```css
    /* ── Hero entrance animations (preserved from index.css) ── */
    @keyframes wkFadeSlideLeft {
      from { opacity: 0; transform: translateX(-20px); }
      to   { opacity: 1; transform: translateX(0); }
    }
    @keyframes wkFadeSlideRight {
      from { opacity: 0; transform: translateX(30px); }
      to   { opacity: 1; transform: translateX(0); }
    }
    @keyframes wkFadeScaleIn {
      from { opacity: 0; transform: scale(0.94); }
      to   { opacity: 1; transform: scale(1); }
    }
    @keyframes wkFadeSlideUp {
      from { opacity: 0; transform: translateY(12px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes wkFadeSlideLeftSm {
      from { opacity: 0; transform: translateX(-12px); }
      to   { opacity: 1; transform: translateX(0); }
    }
    @keyframes wkFadeSlideUpSm {
      from { opacity: 0; transform: translateY(8px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes wkBounceY {
      0%, 100% { transform: translateY(0); }
      50%      { transform: translateY(4px); }
    }
    @keyframes wkFadeSlideUpScroll {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    .wk-anim-fade-left    { will-change: transform, opacity; animation: wkFadeSlideLeft   1s    cubic-bezier(0.22, 1, 0.36, 1) both; }
    .wk-anim-fade-right   { will-change: transform, opacity; animation: wkFadeSlideRight  0.85s cubic-bezier(0.22, 1, 0.36, 1) both; }
    .wk-anim-fade-scale   { will-change: transform, opacity; animation: wkFadeScaleIn     0.7s  cubic-bezier(0.22, 1, 0.36, 1) both; }
    .wk-anim-fade-up      { will-change: transform, opacity; animation: wkFadeSlideUp     0.45s ease both; }
    .wk-anim-fade-left-sm { will-change: transform, opacity; animation: wkFadeSlideLeftSm 0.45s ease both; }
    .wk-anim-fade-up-sm   { will-change: transform, opacity; animation: wkFadeSlideUpSm   0.45s ease both; }
    .wk-anim-bounce-y     { will-change: transform; animation: wkBounceY 2.4s ease-in-out infinite; }

    /* ── Scroll-triggered reveal (replaces framer-motion FadeInSection) ── */
    .wk-reveal {
      opacity: 0;
      transform: translateY(24px);
      will-change: transform, opacity;
      transition: opacity 0.55s cubic-bezier(0.22, 1, 0.36, 1),
                  transform 0.55s cubic-bezier(0.22, 1, 0.36, 1);
    }
    .wk-reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }
    ```

    **3b. Modify `src/index.css`:**

    Replace the entire file content with:

    ```css
    @import './styles/wolknitive-tokens.css';
    @import './styles/wolknitive-base.css';
    @import './styles/wolknitive-animations.css';

    @layer base {
      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
      }
    }
    ```

    This removes: `@import "tailwindcss"`, `@import "tw-animate-css"`, `@plugin "@tailwindcss/typography"`, `@custom-variant dark`, `@theme inline {}`, all `@keyframes`, and all animation classes — they're now in the three dedicated CSS files.

    **3c. Add IntersectionObserver to `index.html`:**

    Insert the following inline `<script>` before the closing `</head>` tag in `index.html`:

    ```html
    <script>
    // IntersectionObserver for scroll-triggered fade-in (replaces framer-motion)
    document.addEventListener('DOMContentLoaded', function() {
      var revealObserver = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            revealObserver.unobserve(entry.target);
          }
        });
      }, { threshold: 0.1, rootMargin: '0px 0px -60px 0px' });

      document.querySelectorAll('.wk-reveal').forEach(function(el) {
        revealObserver.observe(el);
      });

      // Nav scroll tracking (separate observer — D-19)
      var sections = document.querySelectorAll('section[id]');
      var navLinks = document.querySelectorAll('.wk-nav-link');
      var sectionObserver = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          if (entry.isIntersecting) {
            navLinks.forEach(function(link) {
              link.classList.toggle('active', link.getAttribute('data-section') === entry.target.id);
            });
          }
        });
      }, { threshold: 0.3, rootMargin: '0px 0px -100px 0px' });

      sections.forEach(function(section) {
        sectionObserver.observe(section);
      });
    });
    </script>
    ```

    Do NOT modify any other content in index.html (SEO meta tags, OG tags, font preloads, canonical URL, etc.).
  </action>
  <acceptance_criteria>
    - `src/styles/wolknitive-animations.css` exists with all 8 @keyframes and all animation classes
    - `src/index.css` has no `@import "tailwindcss"`, `@import "tw-animate-css"`, `@plugin`, or `@custom-variant` directives
    - `src/index.css` has no `@keyframes` (all moved to animations.css)
    - `src/index.css` imports all three files: `wolknitive-tokens.css`, `wolknitive-base.css`, `wolknitive-animations.css`
    - `index.html` contains `<script>...IntersectionObserver...</script>` before `</head>`
    - The script has both reveal observer and nav section tracking observer
    - `.wk-reveal` class exists in animations.css with `opacity: 0; transform: translateY(24px);`
    - `.wk-reveal.is-visible` exists with `opacity: 1; transform: translateY(0);`
    - `npm run typecheck` passes
    - `npm run dev` starts without errors
  </acceptance_criteria>
  <verify>
    <automated>test -f src/styles/wolknitive-animations.css && grep -c "wkReveal" index.html && grep -c "@keyframes wkFadeSlideLeft" src/styles/wolknitive-animations.css && grep -c "is-visible" src/styles/wolknitive-animations.css</automated>
  </verify>
  <done>
    wolknitive-animations.css created with all @keyframes and scroll-reveal classes. index.html has IntersectionObserver inline script. src/index.css cleaned of Tailwind directives and @keyframes, importing only the three CSS modules. Dev server runs without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html → browser | Inline script crosses into DOM execution context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | IntersectionObserver inline `<script>` in index.html | mitigate | Script is fixed/hardcoded (no dynamic content, no user input). All selectors are class-based and authored by the build. No eval, no innerHTML, no DOM manipulation that could be hijacked. |
| T-01-02 | S (Spoofing) | CSS source files | accept | CSS is hand-authored in source. No user-provided CSS values. All `var(--)` references resolve to controlled custom properties. |
</threat_model>

<verification>
1. `npm run typecheck` passes without errors
2. `npm run dev` starts and serves without console errors
3. `index.html` loads without IntersectionObserver errors in console
4. All 3 CSS files are served by Vite and loaded correctly (check Network tab)
5. All 7 content sections render with correct layout, spacing, colors
6. Responsive breakpoints work at 375px, 768px, 1024px
7. Print stylesheet hides nav/CTA/hero panel in print preview
</verification>

<success_criteria>
- [ ] `wolknitive-tokens.css` has spacing tokens
- [ ] `wolknitive-base.css` exists with all section, card, nav, button, tag, grid, hero, footer, responsive, print classes
- [ ] `wolknitive-animations.css` exists with all @keyframes and scroll-reveal classes
- [ ] `src/index.css` has no Tailwind imports/directives
- [ ] `index.html` has IntersectionObserver inline script
- [ ] `npm run typecheck` passes
- [ ] `npm run dev` runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-architecture/01-01-SUMMARY.md`
</output>
