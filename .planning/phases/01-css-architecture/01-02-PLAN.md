---
phase: 01-css-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 01-css-architecture-01
files_modified:
  - src/pages/home.tsx
  - src/components/FadeIn.tsx
  - package.json
autonomous: true
requirements:
  - DEP-04    # framer-motion removed, replaced with CSS + IntersectionObserver
  - DEP-05    # Tailwind utilities replaced with hand-authored CSS
  - DEP-09    # lucide-react icons replaced with inline SVGs
  - QLT-02    # ~60 inline style objects extracted to CSS classes
  - QLT-03    # onMouseEnter/onMouseLeave handlers replaced with CSS :hover
  - STC-02    # All styling uses hand-authored Wolknitive CSS classes
  - VIZ-01    # Hero section renders identically
  - VIZ-02    # Experience timeline renders identically
  - VIZ-03    # Skills grid renders identically
  - VIZ-04    # Client/industry showcase renders identically
  - VIZ-07    # Scroll-triggered fade-in animations (replacing framer-motion)

must_haves:
  truths:
    - "No inline `style={}` objects remain in home.tsx — all styling is CSS classes"
    - "No Tailwind utility classes remain in home.tsx JSX"
    - "No onMouseEnter/onMouseLeave handlers remain — all hover is CSS :hover"
    - "No framer-motion imports or FadeInSection usage remain"
    - "No lucide-react imports remain — all icons are inline SVGs"
    - "TS color constants (INK, TEAL, V50, etc.) are removed — values come from CSS vars"
    - "All 7 sections render visually identical on dev server"
    - "All hover interactions work via CSS :hover with smooth transitions"
    - "Skill cards have staggered scroll-reveal delays via data-delay attributes"
  artifacts:
    - path: "src/pages/home.tsx"
      provides: "Source file with no inline styles, no Tailwind, no lucide, no framer-motion"
      not_contains: "style=\\{\\{"
    - path: "src/components/FadeIn.tsx"
      provides: "Removed or empty — framer-motion dependency eliminated"
      max_lines: 1
    - path: "package.json"
      provides: "No framer-motion, lucide-react, or tailwind dependencies"
      not_contains: "framer-motion"
    - path: "src/pages/home.tsx"
      provides: "Uses wk-* CSS classes on all elements"
      contains: "wk-container"
  key_links:
    - from: "src/pages/home.tsx"
      to: "src/styles/wolknitive-base.css"
      via: "Element className attributes reference wk- classes"
      pattern: "wk-"
    - from: "src/pages/home.tsx"
      to: "index.html IntersectionObserver script"
      via: "Elements have data-wk-reveal and data-delay attributes consumed by observer"
      pattern: "data-delay"
---

<objective>
Transform home.tsx source to use hand-authored CSS classes instead of inline styles, Tailwind classes, framer-motion, lucide-react, and mouse event handlers. After this plan, the React app still compiles and runs on the dev server, but all visual styling comes from CSS classes defined in Plan 01's woolknitive-base.css and animations.css.

Purpose: Eliminate all framework-specific rendering dependencies from the source code, preparing the way for Phase 2's static build. Every style, interaction, icon, and animation is replaced with its static equivalent.

Output: home.tsx (cleaned), FadeIn.tsx (removed/modified), package.json (dependencies removed)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-css-architecture/01-CONTEXT.md
@.planning/phases/01-css-architecture/01-UI-SPEC.md
@.planning/phases/01-css-architecture/01-RESEARCH.md
@.planning/phases/01-css-architecture/01-PATTERNS.md
@.planning/phases/01-css-architecture/01-VALIDATION.md
@.planning/phases/01-css-architecture/01-01-SUMMARY.md

<interfaces>
**CSS classes available after Plan 01 (complete list):**
- Layout: `.wk-container`, `.wk-section`, `.wk-section-header`, `.wk-section-heading`, `.wk-section-subtitle`
- Grids: `.wk-grid-skills`, `.wk-grid-experience`, `.wk-grid-clients`
- Cards: `.wk-card`, `.wk-card-skills`, `.wk-card-experience`, `.wk-card-client`
- Nav: `.wk-nav`, `.wk-nav-inner`, `.wk-nav-links`, `.wk-nav-link`, `.wk-nav-toggle`
- Buttons: `.wk-btn-cta`, `.wk-btn-outline`
- Tags: `.wk-tag`, `.wk-tag-accent`
- Footer: `.wk-footer`
- Hero: `.wk-hero`, `.wk-hero-inner`, `.wk-hero-content`, `.wk-hero-photo`, `.wk-hero-panel`, `.wk-stat-grid`, `.wk-stat-card`
- Section backgrounds: `.wk-bg-sunken`, `.wk-bg-default`, `.wk-bg-elevated`
- Icons: `.wk-icon-sm`, `.wk-icon-md`, `.wk-icon-lg`
- Animations: `.wk-anim-fade-left`, `.wk-anim-fade-right`, `.wk-anim-fade-scale`, `.wk-anim-fade-up`, `.wk-anim-fade-left-sm`, `.wk-anim-fade-up-sm`, `.wk-anim-bounce-y`, `.wk-reveal`, `.wk-reveal.is-visible`
- Other: `.wk-label`, `.wk-eyebrow`, `.wk-rule`, `.wk-mobile-hidden`, `.wk-btn-text-desktop`, `.wk-scroll-hint`

**Color constant → CSS var mapping:**
INK → var(--ink), TEAL → var(--teal-500), TEAL_6 → var(--teal-600), V50 → var(--vellum-50), V100 → var(--vellum-100), V200 → var(--vellum-200), V300 → var(--vellum-300), V400 → var(--vellum-400), V500 → var(--vellum-500), V700 → var(--vellum-700)

**Existing JSX pattern for reference (home.tsx):**
- 86 inline `style={{}}` objects to replace with className
- 14 onMouseEnter/onMouseLeave handlers to replace with CSS :hover
- 22 lucide-react icon imports to replace with inline <svg>
- 1 framer-motion import + 5 FadeInSection usages to replace with wrapper div + data attributes
- 1 LazyFadeIn wrapper component to remove
- ~10 Tailwind utility class usages to replace with wk- classes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: replace inline styles and Tailwind classes with CSS classes in home.tsx</name>
  <files>src/pages/home.tsx</files>
  <read_first>src/pages/home.tsx (entire file — 1279 lines), src/styles/wolknitive-base.css (classes available), src/styles/wolknitive-tokens.css (token names)</read_first>
  <action>
    Replace all 86 inline `style={}` objects and all ~10 Tailwind utility class usages in `src/pages/home.tsx` with the corresponding `.wk-*` CSS classes.

    **Rules for every replacement:**
    - DO NOT change component structure, JSX hierarchy, text content, i18n keys, or event handler references (event handler replacement comes in Task 2)
    - DO NOT remove import statements (icon/framer removal comes in Task 2)
    - Every `style={{}}` prop must be removed and its styles moved to a className
    - Every `style={}` with a condition/dynamic value must be replaced with a static className + CSS class

    **Replacement table (exhaustive — map each pattern to its wk- class):**

    **Section padding/container patterns:**
    - `<section style={{ padding: "5rem 1.5rem", maxWidth: "72rem", ... }}>` → `<section class="wk-section">` + `<div class="wk-container">` wrapping children. Create a `.wk-section` wrapper around the container. Use `.wk-bg-sunken` if section has `background: var(--vellum-100)`.

    **Section backgrounds:**
    - Know which sections have what bg from home.tsx: Skills (#FAF6EC bg → `wk-bg-default`), Experience (#F3ECD9 bg → `wk-bg-sunken`), Clients (#FAF6EC bg → `wk-bg-default`), CTA (#F3ECD9 → `wk-bg-sunken`)
    - Apply `.wk-bg-sunken` to sections with vellum-100 background
    - Apply `.wk-bg-default` to sections with vellum-50 background

    **Card patterns (repeated 6x each):**
    - Skill cards: `style={{ background: V100, border: 1px solid ${V200}, borderRadius, padding, boxShadow }}` → className="wk-card wk-card-skills"
    - Experience cards: same pattern → className="wk-card wk-card-experience"
    - Client cards: `style={{ background: V50 || V100, ... }}` → className="wk-card wk-card-client"

    **Section header patterns (repeated ~4x):**
    - Each section's <div> containing eyebrow label, heading h2, subtitle p → wrap in `<div class="wk-section-header">`
    - Eyebrow label with `style={{ fontFamily: "var(--font-ui)", ... }}` → span class="wk-label" or class="wk-eyebrow" (use `.wk-label` for the uppercase UI-font label, `.wk-eyebrow` for the italic Bogart teal variant)
    - Section heading h2 → className="wk-section-heading"
    - Section subtitle p → className="wk-section-subtitle"

    **Nav patterns:**
    - Nav outer `<nav>` → className="wk-nav"
    - Nav inner flex container → className="wk-nav-inner" inside `.wk-container`
    - Nav links div (currently `className="hidden md:flex items-center gap-7"`) → className="wk-nav-links"
    - Individual nav buttons → className="wk-nav-link"
    - Add `data-section="hero|skills|experience|clients"` attributes to each nav link (used by IntersectionObserver for active tracking)
    - Mobile nav toggle (currently `className="md:hidden"`) → className="wk-nav-toggle"
    - CTA button text span (currently `className="hidden md:inline"`) → className="wk-btn-text-desktop"
    - Mobile hidden elements (currently `hidden md:block`) → className="wk-mobile-hidden"

    **Button patterns:**
    - Primary CTA (`background: TEAL`, white text) → className="wk-btn-cta"
    - Outline/secondary (border only) → className="wk-btn-outline"
    - Inline text button (nav links) → already .wk-nav-link

    **Hero layout:**
    - Hero section → className="wk-hero"
    - Hero inner flex container → className="wk-hero-inner" inside `.wk-container`
    - Left decorative panel (currently `className="wk-anim-fade-left ... hidden md:block"` + inline style) → className="wk-hero-panel wk-anim-fade-left"
    - Right content column (currently `className="w-full md:w-1/2 ..."` + inline style) → className="wk-hero-content"
    - Left photo column (currently `className="w-full md:w-1/2"`) → className="wk-hero-photo"
    - Stat grid (currently grid with gap) → className="wk-stat-grid"
    - Stat cards (inline styled divs) → className="wk-stat-card"

    **Footer patterns:**
    - Footer outer → className="wk-footer"
    - Content sections → already covered by wk-container

    **Tag/badge patterns:**
    - `<span>` with hairline pill styling → className="wk-tag"
    - Teal accent tags → className="wk-tag wk-tag-accent"

    **Grid patterns (11 Tailwind class usages to replace):**
    - `className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5"` (skills grid) → className="wk-grid-skills"
    - `className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5"` (experience grid) → className="wk-grid-experience"
    - `className="grid grid-cols-2 md:grid-cols-3 gap-4 mb-16"` (clients grid) → className="wk-grid-clients"
    - `className="md:flex-row md:justify-between md:text-left"` (footer) → remove, .wk-footer handles it

    **Bogart italic teal eyebrow in hero:**
    - The `WkEyebrow` or inline element with italic teal styling → className="wk-eyebrow"

    **CTA banner section:**
    - Apply section classes like other sections

    **Icon containers (span wrappers around lucide components):**
    - Wrap each `<IconName size={14|16|20|24}>` in a span with className="wk-icon-sm/md/lg" — the SVGs will be inlined in Task 2

    **Scroll hint chevron:**
    - ChevronDown icon section → className="wk-scroll-hint"

    **After replacement, verify:**
    - `grep -c "style={" src/pages/home.tsx` shows significantly fewer (only remaining should be dynamic style in the nav link active state, which Task 2 will fix)
    - Remove the `className="wk-anim-fade-left" etc. from nav links (those classes animate HERO entrance, not nav)
    - Do NOT change any event handler props yet (Task 2)
    - Do NOT change any icon imports yet (Task 2)
  </action>
  <acceptance_criteria>
    - `grep -c "style=\{" src/pages/home.tsx` returns fewer than 10 (only dynamic inline styles remaining)
    - `grep -c "wk-container" src/pages/home.tsx` returns >= 1
    - `grep -c "wk-card" src/pages/home.tsx` returns >= 6 (card instances)
    - `grep -c "wk-section" src/pages/home.tsx` returns >= 5 (section wrappers)
    - `grep -c "wk-nav" src/pages/home.tsx` returns >= 1
    - `grep -c "wk-btn-cta" src/pages/home.tsx` returns >= 1
    - `grep -c "wk-tag" src/pages/home.tsx` returns >= 1
    - `grep -c "grid grid-cols" src/pages/home.tsx` returns 0 (all Tailwind grid classes removed)
    - `grep -c "hidden md:" src/pages/home.tsx` returns 0 (all Tailwind responsive removed)
    - `grep -c "w-full md:" src/pages/home.tsx` returns 0
    - `npm run typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -c "style=\{" src/pages/home.tsx && grep -c "wk-container" src/pages/home.tsx && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>
    86 inline style objects extracted to CSS classes. All Tailwind utilities replaced with wk- classes. Components wrapped in wk-section/wk-container. Cards use wk-card variants. Nav uses wk-nav class system. Type check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>task 2: replace event handlers, framer-motion, icons, and color constants</name>
  <files>src/pages/home.tsx, src/components/FadeIn.tsx, package.json</files>
  <read_first>src/pages/home.tsx (after task 1 modifications), src/components/FadeIn.tsx, package.json</read_first>
  <action>
    **2a. Remove TS color constants (lines 30-41 of current home.tsx):**
    - Delete the `const INK = "#14110B"` through `const V700 = "#2E261A"` block
    - Throughout the file, replace all remaining references to these constants with the equivalent CSS var():
      - `INK` → no longer needed (all styles extracted in Task 1)
      - `TEAL` → no longer needed
      - `TEAL_6` → no longer needed
      - `V50` → no longer needed
      - etc.
    - If any constant is used in event handlers (e.g., `e.currentTarget.style.color = TEAL`), that gets fixed in step 2b below

    **2b. Replace 14 onMouseEnter/onMouseLeave handlers with CSS :hover (QLT-03):**
    - Remove ALL `onMouseEnter` and `onMouseLeave` props from JSX elements
    - The CSS :hover pseudoclasses are already defined in wolknitive-base.css:
      - Nav links hover: `.wk-nav-link:hover { color: var(--accent); background: var(--accent-soft); }`
      - CTA button hover: `.wk-btn-cta:hover { background: var(--accent-hover); }`
      - Outline button hover: `.wk-btn-outline:hover { background: var(--vellum-100); border-color: var(--fg-3); }`
      - Client cards hover: `.wk-card-client:hover { background: var(--vellum-100); }`
      - Cards hover shadow: `.wk-card:hover { box-shadow: var(--shadow-2); }`

    **2c. Replace framer-motion (DEP-04, VIZ-07):**
    - Remove the `LazyFadeIn` lazy import and wrapper function (around line 63-70)
    - Replace the `FadeInSection` local wrapper function (around line 67-72) with a simple div wrapper:
      ```tsx
      function FadeInSection({ children, delay = 0 }: { children: React.ReactNode; delay?: number }) {
        return (
          <div
            className="wk-reveal"
            style={delay ? { transitionDelay: `${delay}s` } : undefined}
          >
            {children}
          </div>
        );
      }
      ```
    - Remove the `import { lazy } from "react"` from imports if lazy is no longer used
    - Replace `import("../components/FadeIn").then(...)` lazy import with the local wrapper above

    **2d. Replace 22 lucide-react icon imports with inline SVGs (DEP-09):**
    - Remove the entire lucide-react import block: `import { MapPin, Shield, ... } from "lucide-react";`
    - For each icon usage in the JSX, replace `<IconName size={N} />` with an inline SVG element
    - SVG wrapper pattern:
      ```tsx
      <svg class="wk-icon-sm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="..." />
      </svg>
      ```
    - Use the correct size class: 14-16px → `wk-icon-sm`, 20px → `wk-icon-md`, 24px → `wk-icon-lg`
    - Source the exact SVG path `d` attributes from `node_modules/lucide-static/` or the installed `node_modules/lucide-react/` icon source files. For each icon, grep for the path data:
      ```bash
      grep -A5 '"map-pin"\|name: "map-pin"' node_modules/lucide-static/icons/*.json 2>/dev/null | head -10
      ```
      Or read the source `.tsx` files in `node_modules/lucide-react/dist/esm/icons/` to extract the path data.
    - The 22 icons with their correct stroke paths (use lucide source — run the grep to extract actual paths):
      - MapPin (sm), Shield (md), Cloud (md), Brain (md), Users (md), Code2 (md), Lock (md), Globe (sm-md), ChevronDown (sm), Building2 (md), ArrowRight (sm), BarChart3 (lg), Landmark (lg), CreditCard (lg), Smartphone (lg), Activity (lg), ShoppingCart (lg), Zap (sm)

    **2e. Remove framer-motion and lucide-react from package.json:**
    - Remove `"framer-motion"` from both `dependencies` and `devDependencies` in `package.json`
    - Remove `"lucide-react"` from both `dependencies` and `devDependencies`

    **2f. Clean up imports:**
    - Remove unused imports from `home.tsx`:
      - `import { lazy } from "react"` (if no longer used after LazyFadeIn removal)
      - `import { motion } from "framer-motion"` (entire framer-motion import)
      - `import { /* all lucide icons */ } from "lucide-react"`
      - Remove `import { FadeInSection } from "../components/FadeIn"` (replaced by local wrapper) — actually, keep or remove depending on whether FadeInSection is still imported. Since we're defining it locally, remove the import.
    - Check that `import { Changelog } from "../components/Changelog"` remains untouched (Phase 3 removes it)

    **2g. After all replacements, verify by running:**
    ```bash
    npm run typecheck
    grep -c "onMouseEnter\|onMouseLeave" src/pages/home.tsx       # should be 0
    grep -c "lucide-react" src/pages/home.tsx                     # should be 0
    grep -c "framer-motion" src/pages/home.tsx                    # should be 0
    grep -c "style=\{" src/pages/home.tsx                         # should be 0 (or very few for dynamic delay style on wk-reveal)
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "onMouseEnter\|onMouseLeave" src/pages/home.tsx` is 0
    - `grep -c "lucide-react" src/pages/home.tsx` is 0
    - `grep -c "framer-motion" src/pages/home.tsx` is 0
    - `grep -c "style=\{" src/pages/home.tsx` is 0 (the FadeInSection wrapper uses `style` — that's acceptable if it's only the delay, but prefer className with data-delay)
    - `grep -c "framer-motion" package.json` is 0
    - `grep -c "lucide-react" package.json` is 0
    - `grep "const INK\|const TEAL\|const V50\|const V100\|const V200\|const V500" src/pages/home.tsx` returns 0
    - `grep -c "import.*FadeIn" src/pages/home.tsx` is 0
    - `npm run typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5 && grep -c "onMouseEnter\|onMouseLeave" src/pages/home.tsx && grep -c "lucide-react" src/pages/home.tsx && grep -c "framer-motion" package.json</automated>
  </verify>
  <done>
    All 14 onMouseEnter/onMouseLeave handlers replaced with CSS :hover. 22 lucide-react icons replaced with inline SVGs. framer-motion LazyFadeIn replaced with local wk-reveal wrapper. TS color constants removed. package.json dependencies cleaned. Type check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG path data → DOM | Inline SVG elements with stroke paths rendered in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T (Tampering) | Inline SVG elements in home.tsx | mitigate | All SVG path data sourced from lucide-static (trusted npm package). SVG uses `currentColor` for stroke — no hardcoded colors. No user input reaches SVG attributes. |
| T-01-04 | S (Spoofing) | JS event handlers removed | accept | Removing onMouseEnter/onMouseLeave eliminates a class of dynamic style manipulation via `e.currentTarget.style.*` — this is a net security improvement (reduces XSS surface). |
</threat_model>

<verification>
1. `npm run typecheck` passes — no type errors from removed imports or missing dependencies
2. `npm run dev` starts without errors
3. Open browser at localhost — all 7 sections render with correct styles
4. Hover over nav links, CTA buttons, client cards — verify smooth color transitions
5. Scroll down — verify scroll-triggered animations fire once
6. Check no console errors (missing SVG paths, undefined variables)
7. `grep -c "style={" src/pages/home.tsx` — ideally 0
</verification>

<success_criteria>
- [ ] home.tsx has zero inline `style={}` objects
- [ ] home.tsx has zero onMouseEnter/onMouseLeave handlers
- [ ] home.tsx has zero lucide-react imports
- [ ] home.tsx has zero framer-motion imports
- [ ] home.tsx has zero TS color constants (INK, TEAL, etc.)
- [ ] home.tsx uses wk- CSS classes for all styling
- [ ] package.json has no framer-motion or lucide-react dependencies
- [ ] npm run typecheck passes
- [ ] npm run dev renders without console errors
- [ ] All hover interactions work via CSS :hover
- [ ] Section scroll-reveal animations trigger on scroll
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-architecture/01-02-SUMMARY.md`
</output>
